% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_risk_map.R
\name{predict_risk_map}
\alias{predict_risk_map}
\title{Prédire la carte spatiale du risque d'invasion des criquets}
\usage{
predict_risk_map(
  rf_result,
  climat,
  ndvi,
  greenup = NULL,
  seuil_moyen = 0.3,
  seuil_eleve = 0.6
)
}
\arguments{
\item{rf_result}{Liste issue de \code{train_rf_model()}}

\item{climat}{SpatRaster climatique (issu de download_climate_data)}

\item{ndvi}{SpatRaster NDVI (issu de download_ndvi)}

\item{greenup}{SpatRaster greenup optionnel. Par défaut NULL}

\item{seuil_moyen}{Seuil probabilité risque moyen. Par défaut 0.3}

\item{seuil_eleve}{Seuil probabilité risque élevé. Par défaut 0.6}
}
\value{
Une liste contenant :
\item{risque_continu}{Raster probabilité de présence (0-1)}
\item{risque_classe}{Raster classé 1=faible 2=moyen 3=élevé}
\item{stats}{Statistiques de la carte de risque}
}
\description{
Applique le modèle Random Forest directement sur le raster
via terra::predict() —.
Génère une carte de probabilité classée en 3 niveaux de risque.
}
\examples{
\dontrun{
rf   <- train_rf_model(dataset)
clim <- download_climate_data(var = "prec")
ndvi <- download_ndvi(2023, mois = 6)
risk <- predict_risk_map(rf, clim, ndvi)
terra::plot(risk$risque_continu, main = "Probabilite presence")
terra::plot(risk$risque_classe,  main = "Carte de risque")
}

}
